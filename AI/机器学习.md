# 线性回归核心概念

方程解析：
>y = mx + b


在这个汽车重量与油耗的例子中：

y (因变量) = MPG（每加仑英里数）← 我们要预测的目标
x (自变量) = 汽车重量（磅）← 输入特征
m (斜率) = 重量每增加1磅，MPG 的变化量（应为负值）
b (截距) = 当重量为0时的理论 MPG 值

这里的核心就是：计算出 b 和 m 。通过大量数据讽刺b 和 m 的值，得到一个：拟合线

损失计算：最佳结果与实际结果的差值。肯定是越小越好，但要考虑可能有离群点。
梯度下降法
超参数

