我要将此页面高还原转换为 HTML 页面，请按以下要求输出完整设计描述，方便后续AI生成代码：

1. 视觉细节
请详细列出页面所有视觉参数，包括但不限于：
- 字体名称
- 标题字号、正文字号
- 字重（粗细）
- 行高
- 字体颜色
- 背景颜色（色号）
- 按钮颜色（默认 / Hover / 点击）
- 边框颜色与粗细
- 圆角大小
- 阴影效果
- 模块之间间距（padding / margin）

2. 页面布局结构
请按从上到下顺序描述页面结构，包括：
- 顶部导航栏（Logo、菜单、按钮等）
- Banner / Hero区域
- 内容模块（功能区、卡片区、介绍区等）
- 底部Footer

并说明每个区域：
- 宽度
- 高度
- 左右布局 / 上下布局
- 栅格列数
- 元素对齐方式

3. 组件细节
对于按钮、输入框、卡片、图片区域等组件，请说明：
- 尺寸
- 状态变化
- 图标位置
- 文本位置

4. 响应式适配
请说明移动端、平板端如何变化：
- 是否改单列
- 字号缩小多少
- 导航是否折叠

5. 输出格式
请使用 Markdown 输出，结构清晰，方便我交给 AI 生成 HTML + CSS + JS。